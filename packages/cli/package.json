{
  "name": "@repo/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "fortuna": "dist/main.js"
  },
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"No lint configured for @repo/cli yet\"",
    "test": "tsx --test test/**/*.test.ts",
    "clean": "rm -rf dist"
  }
  ,
  "devDependencies": {
    "@types/node": "^25.7.0",
    "tsx": "^4.21.0"
  }
}
