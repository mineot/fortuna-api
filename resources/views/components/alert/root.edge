@let(variant = $props.get('variant') || 'default')
@inject({ variant, autoDismiss: $props.get('autoDismiss') })
@let(mappedVariant = variant === 'destructive' ? 'danger' : variant)
@let(classes = ['alert', `alert-${mappedVariant}`, 'shadow-sm'])

<div {{ $props
  .except(['variant', 'autoDismiss'])
  .merge({
    role: 'alert',
    class: classes,
    'x-data': 'alert',
    'x-show': 'isVisible',
    'x-cloak': true
  })
  .toAttrs()
}}> {{{ await $slots.main() }}} </div>
