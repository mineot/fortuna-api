# Fortuna Project - Current Context

## 1) Project objective
- Name: `fortuna`
- Purpose: personal financial management application.
- Current state: desktop application bootstrap with Electron + TypeScript.

## 2) Decisions already made
- Chosen license: Apache 2.0.
- Primary language: TypeScript.
- JavaScript module system: ESM (`"type": "module"`).
- Minimum Node.js version: `>=20.0.0`.
- Build output directory: `dist/`.
- npm distribution as a single bundle via `tsup` in `dist/fortuna.js` (minified, no sourcemap).
- Main and preload are bundled/minified by `tsup` (`dist/fortuna.js` and `dist/preload.cjs`).
- Code quality stack: `ESLint` (modern flat config) + `Prettier`.
- Renderer stack: `React + TypeScript + React Router` on top of Vite.
- Renderer styling stack: `Tailwind CSS` via semantic classes defined in CSS with `@apply`.
- Electron is used as desktop runtime across operating systems, without generating per-OS binaries.
- Database layer initialized with `Kysely` + `SQLite` (`better-sqlite3`).
- Database access uses a singleton instance (`getDb`) in the Electron main process lifecycle.
- Migration system implemented with `up/down` and numeric versions.

## 3) Licensing
- License file created: `LICENSE`.
- `package.json` updated with `"license": "Apache-2.0"`.
- Copyright:
  - `Copyright 2026 Fabiano Pires (Mineot)`.

## 4) Current file structure
- `package.json`: project metadata and scripts.
- `package-lock.json`: npm lockfile.
- `tsconfig.json`: TypeScript compiler config.
- `tsconfig.eslint.json`: dedicated ESLint config (type-aware lint including `tsup.config.ts`).
- `eslint.config.mjs`: modern lint configuration with Prettier integration.
- `.prettierrc`: formatting rules.
- `.prettierignore`: Prettier exclusions.
- `src/main.ts`: current entrypoint.
- `src/preload.ts`: typed preload source bundled by `tsup` for production.
- `src/preload.cjs`: preload file used directly in development runtime.
- `src/shared/ipc.ts`: shared IPC channels and renderer API contracts (type-safe across main/renderer).
- `src/shared/handlers/types.handler.ts`: IPC handlers related to `types` table.
- `src/shared/handlers/index.ts`: central IPC handler registration (`registerIpcHandlers`).
- `src/database/schema/index.ts`: central Database schema and type re-exports.
- `src/database/schema/migrations.schema.ts`: `migrations` table schema type.
- `src/database/schema/types.schema.ts`: `types` table schema type and model aliases (`NewType`, `Types`, `TypeUpdate`).
- `src/database/client.ts`: Kysely SQLite factory + singleton access (`getDb`) + cleanup (`destroyDb`).
- `src/database/index.ts`: database module exports.
- `src/database/migrations-table.ts`: ensures the base `migrations` table exists (non-versioned prerequisite).
- `src/database/migrator.ts`: migration runner by target version (`migrateTo` / `migrateToLatest`).
- `src/database/migrations/types.ts`: migration contract type.
- `src/database/migrations/index.ts`: ordered migration registry.
- `src/database/migrations/001_create_types_table.ts`: versioned migration creating `types` table.
- `src/renderer/index.html`: Vite renderer entry HTML.
- `src/renderer/src/main.tsx`: Vite renderer bootstrap script (React).
- `src/renderer/src/App.tsx`: app root with router integration.
- `src/renderer/src/router/index.tsx`: route declarations.
- `src/renderer/src/components/layout/AppShell.tsx`: shared layout/shell.
- `src/renderer/src/pages/HomePage.tsx`: home page.
- `src/renderer/src/pages/TypesPage.tsx`: types listing page.
- `src/renderer/src/services/types.service.ts`: renderer service calling preload API.
- `src/renderer/src/styles/global.css`: global renderer styles.
- `src/renderer/src/vite-env.d.ts`: renderer global typing for preload API.
- `src/renderer/vite.config.mjs`: Vite config for renderer subproject.
- `scripts/copy-renderer.mjs`: copies full renderer build output (`src/renderer/dist`) to root `dist/`.
- `LICENSE`: Apache License 2.0 text.
- `.codex`: this project context.
- `.gitignore`: ignores `dist/`, `node_modules/`, and `dev.*`.
- Historical note: entrypoint was renamed from `src/index.ts` to `src/main.ts`.

## 5) Installed dev dependencies
- `@types/better-sqlite3` (`^7.6.13`)
- `@electron/rebuild` (`^4.0.1`)
- `@types/node` (`^25.5.2`)
- `@typescript-eslint/eslint-plugin` (`^8.58.1`)
- `@typescript-eslint/parser` (`^8.58.1`)
- `cross-env` (`^7.0.3`)
- `concurrently` (`^9.2.1`)
- `eslint` (`^10.2.0`)
- `eslint-config-prettier` (`^10.1.8`)
- `eslint-plugin-simple-import-sort` (`^12.1.1`)
- `globals` (`^17.4.0`)
- `prettier` (`^3.8.1`)
- `tsup` (`^8.5.1`)
- `tsx` (`^4.21.0`)
- `typescript` (`^6.0.2`)
- `vite` (`^7.1.7`)
- `wait-on` (`^8.0.5`)
- `@vitejs/plugin-react` (`^5.1.0`)
- `@types/react` (`^19.2.2`)
- `@types/react-dom` (`^19.2.2`)
- `tailwindcss` (`^3.4.17`)
- `postcss` (`^8.5.6`)
- `autoprefixer` (`^10.4.21`)

## 5.1) Installed runtime dependencies
- `better-sqlite3` (`^12.8.0`)
- `electron` (`^41.2.0`)
- `kysely` (`^0.28.15`)
- `react` (`^19.2.0`)
- `react-dom` (`^19.2.0`)
- `react-router-dom` (`^7.9.4`)

## 6) Configured npm scripts
- `build`: `npm run build:renderer && tsup && node scripts/copy-renderer.mjs`
- `build:renderer`: `vite build --config src/renderer/vite.config.mjs`
- `dev`: `concurrently -k -n renderer,electron -c cyan,green "npm run dev:renderer" "npm run dev:electron"`
- `dev:renderer`: `vite --config src/renderer/vite.config.mjs`
- `dev:electron`: `wait-on tcp:127.0.0.1:5173 && cross-env NODE_ENV=dev NODE_OPTIONS=--import=tsx electron --no-sandbox --disable-setuid-sandbox src/main.ts`
- `lint`: `eslint .`
- `lint:fix`: `eslint . --fix`
- `format`: `prettier . --write`
- `format:check`: `prettier . --check`
- `postinstall`: `npm run rebuild:electron`
- `start`: `electron --no-sandbox --disable-setuid-sandbox dist/fortuna.js`
- `rebuild:electron`: `electron-rebuild -f -w better-sqlite3`
- `check`: `tsc --noEmit -p tsconfig.json`

## 7) TypeScript configuration (tsconfig)
- Modern ECMAScript target: `target: ESNext`
- Modern library set: `lib: ["ESNext", "DOM"]`
- Node module format: `module: ESNext`
- Module resolution: `moduleResolution: Bundler`
- JSX transform: `jsx: react-jsx`
- Paths alias:
  - `@db` -> `./src/database/index.ts`
  - `@db/schema` -> `./src/database/schema/index.ts`
  - `@shared/*` -> `./src/shared/*`
- Strict mode enabled: `strict: true`
- Additional safety flags:
  - `noUncheckedIndexedAccess`
  - `exactOptionalPropertyTypes`
  - `isolatedModules`
  - `verbatimModuleSyntax`
- Local TypeScript imports are written without `.js` extension (e.g., `./database`, `./client`, `./schema`).
- Artifact generation:
  - `declaration: true`
  - `declarationMap: true`
  - `sourceMap: true`
- Directories:
  - input: `src/`
  - output: `dist/`
- TypeScript project includes renderer files:
  - `src/**/*.ts`
  - `src/**/*.tsx`
  - `src/**/*.d.ts`

## 8) Known notes
- npm distribution build:
- Entry: `src/main.ts`
- Single output file: `dist/fortuna.js`
- Minification: enabled
- Sourcemap: disabled
- Build copies all renderer build assets from `src/renderer/dist` to root `dist`.
- `npm run dev` starts Vite first and then Electron.
- Dev TypeScript loading is done via `NODE_OPTIONS='--import tsx'`.
- In dev (`NODE_ENV=dev`), Electron loads `http://127.0.0.1:5173`.
- `npm run start` opens Electron using built files in root `dist/` (`fortuna.js` + Vite output).
- Vite renderer uses `base: './'` so built asset paths work under Electron `file://` on `npm run start`.
- Vite renderer config maps aliases for renderer runtime:
- `@db/schema` -> `src/database/schema/index.ts`
- `@shared` -> `src/shared`
- `server.fs.allow` includes `src/` parent so renderer can import shared/database type modules.
- Renderer data flow is prepared via IPC:
- main process registers handlers by module via `registerIpcHandlers(db)`.
- `types:list` is handled inside `src/shared/handlers/types.handler.ts`.
- preload exposes `window.fortuna.listTypes()`.
- Development uses `src/preload.cjs` directly for Electron preload reliability.
- Production uses bundled/minified `dist/preload.cjs` generated from `src/preload.ts`.
- renderer consumes typed payload (`Types[]`) without direct DB access.
- Tailwind usage rule:
- no utility classes directly in TSX/HTML markup.
- semantic class names are defined in `global.css` using `@apply`.
- renderer code should remain TypeScript-only (`.ts`/`.tsx`).
- renderer shows explicit fallback message if preload API is unavailable, avoiding silent blank screen.
- In Linux environments without configured `chrome-sandbox` SUID, dev/start run with `--no-sandbox --disable-setuid-sandbox`.
- `better-sqlite3` must be rebuilt for Electron ABI (`npm run rebuild:electron`) after install/version changes.
- Rebuild is automated on install via `postinstall`.
- Electron was bumped to a secure major version line to address `npm audit` high-severity advisories.
- Database path resolution by `NODE_ENV`:
- `dev`: `path.join(process.cwd(), 'dev.db')`
- default (no `NODE_ENV=dev`): `~/.fortuna/fortuna.db` (directory is created automatically)
- Migrations:
- `migrations` table is always created as prerequisite (not versioned migration).
- Tracks `version`, `name`, and `executed_at`.
- Supports automatic `up` and `down` synchronization based on numeric version comparison between DB and migration files.
- If DB version is lower than the latest migration file version, pending `up` migrations are applied automatically.
- If DB version is higher than the latest migration file version, `down` is applied automatically until synchronization.
- If a DB-applied version no longer has a migration file, its registry row is removed from `migrations` to allow auto-sync with current code state.
- Current versioned migration:
- `v1` (`create_types_table`) creates table `types` with:
  - `id` generated (`integer primary key autoincrement`)
  - `group` (`text`)
  - `value` (`text`)
- Database lifecycle:
- instance is created lazily via `getDb()` and reused (single process instance)
- app quit is intercepted in `before-quit` to await `destroyDb()` before final shutdown
- Schema pattern:
- all DB schemas are split inside `src/database/schema/*` and composed in `src/database/schema/index.ts`.
- with `verbatimModuleSyntax`, type-only re-exports must use `export type`.
- Lint and format should run before npm publish.
- Type-aware ESLint uses `tsconfig.eslint.json` to avoid files outside `rootDir` from `tsconfig.json`.
- Current Prettier rules:
- `singleQuote: true`
- `trailingComma: "all"`
- `semi: true`
- `printWidth: 100`
- `tabWidth: 2`
- Import order is enforced automatically by ESLint (`simple-import-sort`) and can be fixed with `npm run lint:fix`.

## 9) Conventions for next changes
- Keep TypeScript sources in `src/` and build output in `dist/`.
- Prefer modern configuration (`ESNext`) unless compatibility requires otherwise.
- Mandatory rule: always update this file (`.codex`) whenever architecture, scripts, tooling, folder structure, or build/runtime flow changes.
- Mandatory rule: after any code change, always run `npm run lint:fix`.
- Mandatory rule: renderer implementation must use TypeScript files (`.ts` / `.tsx`), not `.js` / `.jsx`.

## 10) Collaboration flow (terminal)
- Agreed flow:
- The user runs only installation commands when requested (`npm install`, `npm i`, `npm install -D`, etc.).
- The agent performs all other configuration and file changes directly in the project.
